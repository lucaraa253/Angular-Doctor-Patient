import { SpecialtySelectPipe } from './specialty-select.pipe';

describe('SpecialtySelectPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialtySelectPipe();
    expect(pipe).toBeTruthy();
  });
});
