import { InsuranceSelectPipe } from './insurance-select.pipe';

describe('InsuranceSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new InsuranceSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
